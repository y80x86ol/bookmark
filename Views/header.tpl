	<!--<div class="header">
		{if $user['username']}
			欢迎回来,{$user['username']}|<a href="?control=user&action=logout">退出登录</a>
		{else}
		<a href="?control=user&action=login">登录</a>|<a href="?control=user&action=register">注册</a>
		{/if}
	</div>-->
	<!--<div class="header">
		{if $user['username']}
			欢迎回来,{$user['username']}|<a href="/user/logout">退出登录</a>
		{else}
		<a href="/user/login">登录</a>|<a href="/user/register">注册</a>
		{/if}
	</div>-->
	